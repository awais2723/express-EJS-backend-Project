<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <title>Edit Product</title>
</head>
<body>
  <div class="container">
    <h1>Edit Product</h1>
    <form action="/edit/<%= product.id %>" method="POST" enctype="multipart/form-data">
      <div class="form-group">
        <label for="name">Product Name:</label>
        <input type="text" class="form-control" id="name" name="name" value="<%= product.name %>" required>
      </div>
      <div class="form-group">
        <label for="price">Price:</label>
        <input type="text" class="form-control" id="price" name="price" value="<%= product.price %>" required>
      </div>
      <div class="form-group">
        <label>Availability:</label><br>
        <div class="form-check form-check-inline">
          <input class="form-check-input" type="radio" name="availability" id="available" value="Available" <%= product.availability === 'Available' ? 'checked' : '' %>>
          <label class="form-check-label" for="available">Available</label>
        </div>
        <div class="form-check form-check-inline">
          <input class="form-check-input" type="radio" name="availability" id="notAvailable" value="Not Available" <%= product.availability === 'Not Available' ? 'checked' : '' %>>
          <label class="form-check-label" for="notAvailable">Not Available</label>
        </div>
      </div>
      <div class="form-group">
        <label for="currentPicture">Current Picture:</label><br>
        <% if (product.picture) { %>
          <img src="/uploads/<%= product.picture %>" width="300" class="img-thumbnail"><br>
        <% } else { %>
          <p>No picture available</p>
        <% } %>
      </div>
      <div class="form-group">
        <label for="picture">Change Picture:</label>
        <input type="file" class="form-control-file" id="picture" name="picture">
      </div>
      <div class="form-group">
        <label for="currentBrochure">Current Brochure:</label><br>
        <% if (product.brochure) { %>
          <a href="/uploads/<%= product.brochure %>" target="_blank">View Brochure</a>
        <% } else { %>
          <p>No brochure available</p>
        <% } %>
      </div>
      <div class="form-group">
        <label for="brochure">Change Brochure (PDF):</label>
        <input type="file" class="form-control-file" id="brochure" name="brochure">
      </div>
      <button type="submit" class="btn btn-primary">Update Product</button>
      <a href="/" class="btn btn-secondary">Cancel</a>
    </form>
  </div>
</body>
</html>
